<?xml version="1.0" encoding="utf-8"?>
<odoo>


    <record id="external_agent_acquirer_action" model="ir.actions.act_window">
        <field name="name">External Agent Acquirer</field>
        <field name="res_model">external.agent.acquirer</field>
        <field name="view_mode">tree,form</field>
    </record>

    <record id="act_agent_to_endpoints" model="ir.actions.act_window">
        <field name="name">Endpoints</field>
        <field name="res_model">external.agent.endpoint</field>
        <field name="view_mode">kanban,form</field>
        <!-- <field name="context">{'': active_id}</field> -->
        <!-- <field name="groups_id" eval="[(4, ref('sales_team.group_sale_salesman'))]"/> -->
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new quotation, the first step of a new Endpoint!
            </p>
            <!-- <p>
                Once the quotation is confirmed by the customer, it becomes a sales order.<br/>
                You will be able to create an invoice and collect the payment.
            </p> -->
        </field>
    </record>

</odoo>
